Note:

For the codes, compile them separately (backwardRecursion.c and forwardRecursion.c) using gcc to get object files and then execute them.
To recreate the pdf file (HW1) from the Tex file, use pdfLatex.